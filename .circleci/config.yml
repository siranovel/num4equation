version: 2.1
orbs:
  ruby: circleci/ruby@1.0.4
jobs:
  deploy:
    docker:
      - image: cimg/ruby:2.7
    steps:
      - checkout
      - ruby/install-deps
      - run:
          name: Publish gem to Rubygems
          command: |
            echo "Hello world"
workflows:
  version: 2.1
  my-workflow:
     jobs:
       - deploy
